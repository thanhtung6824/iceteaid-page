import styled from 'styled-components';
import { DialogContent } from './CommonDialog.styled';

export const VerifyDialogContent = styled(DialogContent)`
    img {
        width: 3em;
        height: 3em;
    }
`;

export const ButtonField = styled.div`
    margin-top: 3em;
`;
