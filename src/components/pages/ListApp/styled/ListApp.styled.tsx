import styled from 'styled-components';
import { DarkModeGroup } from '../../../layouts/styled/Header.styled';

export const ToggleGroup = styled(DarkModeGroup)`
    float: right;
    margin-right: 3em;
`;
